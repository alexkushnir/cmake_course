project(advancedmath)

# Create shared library (slide 102)

# Add include directories of basic math (slide 82)
# Add link directories of basic math (similar to -L) - google target_link_directories
# Add link library of basic math (similar to -l) (slide 147)